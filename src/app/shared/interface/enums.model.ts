export enum FormNames {
    FromControl = 'fromControl',
    ToControl = 'toControl',
    AmountControl = 'amountControl',
}

export enum TableColumnNames {
    Date = 'date',
    ExchangeRate = 'exchangeRate',
    Name = 'name',
    Summary = 'summary',
}

export enum LocalStorageItems {
    SelectedTimeInterval = 'selected-time-interval',
    ExchangeRates = 'exchangeRates',
}

export enum TimeIntervalTypes {
    AllTime = 'allTime',
    SevenDays = 'sevenDays',
    FourteenDays = 'fourteenDays',
    ThirtyDaysConversions = 'thirtyDaysConversions',
}

export enum Currency {
    USD = 'USD',
}

export enum StatisticalDataTableFields {
    Lowest = 'Lowest',
    Highest = 'Highest',
    Average = 'Average',
}
